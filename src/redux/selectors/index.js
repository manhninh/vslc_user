// another
export const isLoading = state => state.another.isLoading;

export const popupError = state => state.another.popupError;

export const popupSuccess = state => state.another.popupSuccess;

export const routeName = state => state.another.routeName;

export const customers = state => state.another.customers;

//category
export const listCategory = state => state.category.listCategory;

//publicc
export const listNotifications = state => state.publicc.listNotifications;

export const isLoadListNotifi = state => state.publicc.isLoadListNotifi;

//service
export const listHistoryService = state => state.service.listHistoryService;

export const listFavatiteStaff = state => state.service.listFavatiteStaff;

export const ordersId = state => state.service.ordersId;

export const ordersDetailId = state => state.service.ordersDetailId;
